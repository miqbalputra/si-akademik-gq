<x-layouts.portal title="Referensi RPP" portalLabel="Portal Guru" breadcrumb="Referensi RPP">
    <header class="portal-page-header"><div><p class="school-index">Perangkat Pembelajaran</p><h1 class="text-slate-900">Referensi RPP</h1><p class="mt-2 text-sm text-slate-500">Gunakan RPP guru lain untuk mapel dan kelas yang juga Anda ampu.</p></div><a class="btn btn-outline" href="{{ route('guru.rpp.index') }}">RPP Saya</a></header>
    <form class="ui-card mb-6 flex gap-3 rounded-2xl p-4" method="GET"><input class="form-input flex-1" name="q" value="{{ request('q') }}" placeholder="Cari materi"><button class="btn btn-outline">Cari</button></form>
    <div class="grid gap-4 md:grid-cols-2">@forelse($rpps as $rpp)<article class="ui-card rounded-2xl p-5"><h2 class="font-black text-slate-800">{{ $rpp->materi }}</h2><p class="mt-1 text-sm text-slate-500">{{ $rpp->classSubject?->subject?->name }} · {{ $rpp->classSubject?->classroomTerm?->name }} · {{ $rpp->teacher?->name }}</p>@if($rpp->isStructured())<form class="mt-4" method="POST" action="{{ route('guru.rpp.duplicate', $rpp) }}">@csrf<button class="btn btn-primary btn-sm">Duplikat sebagai draft</button></form>@endif</article>@empty<p class="text-slate-500">Belum ada referensi yang sesuai penugasan Anda.</p>@endforelse</div><div class="mt-5">{{ $rpps->links() }}</div>
</x-layouts.portal>
